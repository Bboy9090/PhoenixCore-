# Root Expo / pnpm template — relocated

The template now lives at **`experimental/root-app-template/`**.

```bash
cd experimental/root-app-template
pnpm install
pnpm dev
```

This is **not** [`phoenix-core-mobile/`](phoenix-core-mobile/).
