# Phoenix Archive

`archive/` is for reviewed material that should be preserved but removed from active development paths.

Archive policy:

- Preserve useful old work before deleting it.
- Delete only obvious generated junk, dependencies, caches, and packaged outputs.
- Keep archive moves small enough to review.
- Record source moves in `docs/archive-manifest.md`.

PR 3 does not move additional source into archive.
