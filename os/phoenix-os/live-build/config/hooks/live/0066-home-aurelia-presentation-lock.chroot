#!/bin/sh
set -eu

LIVE_USER="phoenix"
LIVE_HOME="/home/${LIVE_USER}"
WALLPAPER_PATH="/usr/share/images/desktop-base/desktop-background.png"
LOGO_SOURCE="/usr/share/sddm/themes/phoenix/logo.png"
PRESENTATION_DIR="/usr/share/bwos/presentation"
PRESENTATION_LOCK="${PRESENTATION_DIR}/home-aurelia.lock"
PRESENTATION_JSON="/etc/bwos/presentation-layer.json"
COLOR_SCHEME="/usr/share/color-schemes/HomeAurelia.colors"
ICON_THEME_DIR="/usr/share/icons/home-aurelia"
KDEGLOBALS="/etc/skel/.config/kdeglobals"
PLASMARC="/etc/skel/.config/plasmarc"
THEME_PACK_DIR="/usr/share/home-aurelia-theme-pack"
KWINRC="/etc/skel/.config/kwinrc"
KCMINPUTRC="/etc/skel/.config/kcminputrc"
KVANTUMRC="/etc/skel/.config/Kvantum/kvantum.kvconfig"

install -d -m 0755 /etc/bwos "${PRESENTATION_DIR}" /etc/skel/.config /etc/xdg
install -d -m 0755 /usr/share/color-schemes
install -d -m 0755 "${ICON_THEME_DIR}/512x512/apps" "${ICON_THEME_DIR}/512x512/places"

cat > "${PRESENTATION_LOCK}" <<'EOF_LOCK'
Home Aurelia presentation layer is the canonical alpha desktop identity.
Internal edition ids, build ids, package ids, service ids, and paths remain unchanged.
Boot/session determinism remains the release gate; presentation assets are not proof of desktop success.
EOF_LOCK

cat > "${PRESENTATION_JSON}" <<EOF_JSON
{
  "schema_version": 1,
  "identity": "home-aurelia",
  "public_name": "Home Aurelia OS",
  "tagline": "Four Legacies. One Throne.",
  "scope": "alpha-presentation-layer",
  "internal_edition_id_renamed": false,
  "controlled_layers": {
    "kde_shell": "HomeAurelia color scheme plus inherited Breeze shell behavior",
    "plymouth": "/usr/share/plymouth/themes/phoenix",
    "sddm": "/usr/share/sddm/themes/phoenix",
    "wallpaper": "${WALLPAPER_PATH}",
    "icon_pack": "home-aurelia, inheriting Breeze and hicolor",
    "splash_assets": "Plymouth and SDDM phoenix theme assets",
    "taskbar_menu_accents": "KDE color scheme active/accent colors"
  },
  "evidence_required": [
    "BWOS_DESKTOP_SESSION_STARTED",
    "BWOS_WALLPAPER_APPLIED"
  ],
  "release_candidate": false
}
EOF_JSON

cat > "${COLOR_SCHEME}" <<'EOF_COLORS'
[ColorScheme]
Name=HomeAurelia
shadeSortColumn=true

[Colors:Button]
BackgroundAlternate=16,24,39
BackgroundNormal=10,18,38
DecorationFocus=47,128,255
DecorationHover=122,200,255
ForegroundActive=47,128,255
ForegroundInactive=144,161,185
ForegroundLink=122,200,255
ForegroundNegative=197,57,53
ForegroundNeutral=212,169,79
ForegroundNormal=248,250,252
ForegroundPositive=59,179,115
ForegroundVisited=122,200,255

[Colors:Selection]
BackgroundAlternate=47,128,255
BackgroundNormal=47,128,255
DecorationFocus=122,200,255
DecorationHover=122,200,255
ForegroundActive=255,255,255
ForegroundInactive=229,238,249
ForegroundLink=255,255,255
ForegroundNegative=255,190,190
ForegroundNeutral=255,221,150
ForegroundNormal=255,255,255
ForegroundPositive=190,255,220
ForegroundVisited=255,255,255

[Colors:Tooltip]
BackgroundAlternate=16,24,39
BackgroundNormal=5,7,10
DecorationFocus=47,128,255
DecorationHover=212,169,79
ForegroundActive=47,128,255
ForegroundInactive=144,161,185
ForegroundLink=122,200,255
ForegroundNegative=197,57,53
ForegroundNeutral=212,169,79
ForegroundNormal=248,250,252
ForegroundPositive=59,179,115
ForegroundVisited=122,200,255

[Colors:View]
BackgroundAlternate=16,24,39
BackgroundNormal=5,7,10
DecorationFocus=47,128,255
DecorationHover=212,169,79
ForegroundActive=47,128,255
ForegroundInactive=144,161,185
ForegroundLink=122,200,255
ForegroundNegative=197,57,53
ForegroundNeutral=212,169,79
ForegroundNormal=248,250,252
ForegroundPositive=59,179,115
ForegroundVisited=122,200,255

[Colors:Window]
BackgroundAlternate=16,24,39
BackgroundNormal=5,7,10
DecorationFocus=47,128,255
DecorationHover=212,169,79
ForegroundActive=47,128,255
ForegroundInactive=144,161,185
ForegroundLink=122,200,255
ForegroundNegative=197,57,53
ForegroundNeutral=212,169,79
ForegroundNormal=248,250,252
ForegroundPositive=59,179,115
ForegroundVisited=122,200,255

[General]
ColorScheme=HomeAurelia
Name=HomeAurelia

[KDE]
contrast=4

[WM]
activeBackground=5,7,10
activeBlend=47,128,255
activeForeground=248,250,252
inactiveBackground=16,24,39
inactiveBlend=144,161,185
inactiveForeground=144,161,185
EOF_COLORS

cat > "${ICON_THEME_DIR}/index.theme" <<'EOF_ICON'
[Icon Theme]
Name=Home Aurelia
Comment=Home Aurelia alpha icon layer; inherits Breeze for complete daily-use coverage.
Inherits=breeze-dark,breeze,hicolor
Directories=512x512/apps,512x512/places

[512x512/apps]
Size=512
Context=Applications
Type=Fixed

[512x512/places]
Size=512
Context=Places
Type=Fixed
EOF_ICON

if [ -f "${LOGO_SOURCE}" ]; then
    cp "${LOGO_SOURCE}" "${ICON_THEME_DIR}/512x512/apps/distributor-logo.png"
    cp "${LOGO_SOURCE}" "${ICON_THEME_DIR}/512x512/apps/start-here-kde.png"
    cp "${LOGO_SOURCE}" "${ICON_THEME_DIR}/512x512/places/user-home.png"
fi

cat > "${KDEGLOBALS}" <<'EOF_KDEGLOBALS'
[General]
ColorScheme=HomeAurelia
Name=HomeAurelia
TerminalApplication=konsole

[Icons]
Theme=home-aurelia

[KDE]
LookAndFeelPackage=org.kde.breezedark.desktop
SingleClick=false
widgetStyle=Kvantum

[WM]
activeBackground=5,7,10
activeBlend=47,128,255
activeForeground=248,250,252
inactiveBackground=16,24,39
inactiveBlend=144,161,185
inactiveForeground=144,161,185
EOF_KDEGLOBALS

cat > "${PLASMARC}" <<EOF_PLASMA
[General]
ColorScheme=HomeAurelia
Name=HomeAurelia
wallpaper=${WALLPAPER_PATH}

[Theme]
name=default
EOF_PLASMA

# Copy system-level files from the theme pack if present
if [ -d "${THEME_PACK_DIR}" ]; then
    echo "Staging system-level assets from theme pack..."
    
    # 1. Colors
    if [ -d "${THEME_PACK_DIR}/06-Color-Schemes" ]; then
        mkdir -p /usr/share/color-schemes
        cp -f "${THEME_PACK_DIR}"/06-Color-Schemes/*.colors /usr/share/color-schemes/ || true
    fi
    
    # 2. Kvantum themes
    if [ -d "${THEME_PACK_DIR}/07-Kvantum" ]; then
        mkdir -p /usr/share/Kvantum
        cp -Rf "${THEME_PACK_DIR}"/07-Kvantum/HomeAurelia /usr/share/Kvantum/ || true
    fi
    
    # 3. Aurorae Borders
    if [ -d "${THEME_PACK_DIR}/08-Window-Decorations/Aurorae" ]; then
        mkdir -p /usr/share/aurorae/themes
        cp -Rf "${THEME_PACK_DIR}"/08-Window-Decorations/Aurorae/HomeAurelia /usr/share/aurorae/themes/ || true
    fi
    
    # 4. Icons
    if [ -d "${THEME_PACK_DIR}/09-Icons" ]; then
        mkdir -p /usr/share/icons/home-aurelia
        cp -Rf "${THEME_PACK_DIR}"/09-Icons/* /usr/share/icons/home-aurelia/ || true
    fi
    
    # 5. Cursors
    if [ -d "${THEME_PACK_DIR}/10-Cursors" ]; then
        mkdir -p /usr/share/icons/home-aurelia-cursors
        cp -Rf "${THEME_PACK_DIR}"/10-Cursors/* /usr/share/icons/home-aurelia-cursors/ || true
    fi
    
    # 6. SDDM Login Theme
    if [ -d "${THEME_PACK_DIR}/11-SDDM-Login/HomeAurelia" ]; then
        mkdir -p /usr/share/sddm/themes
        cp -Rf "${THEME_PACK_DIR}"/11-SDDM-Login/HomeAurelia /usr/share/sddm/themes/ || true
    fi
    
    # 7. GRUB Theme
    if [ -d "${THEME_PACK_DIR}/12-GRUB/HomeAurelia" ]; then
        mkdir -p /boot/grub/themes
        cp -Rf "${THEME_PACK_DIR}"/12-GRUB/HomeAurelia /boot/grub/themes/ || true
    fi
    
    # 8. Sound scheme
    if [ -d "${THEME_PACK_DIR}/13-Sounds" ]; then
        mkdir -p /usr/share/sounds/home-aurelia
        cp -Rf "${THEME_PACK_DIR}"/13-Sounds/* /usr/share/sounds/home-aurelia/ || true
    fi
    
    # 9. Konsole Terminal colorscheme profile
    if [ -d "${THEME_PACK_DIR}/17-Terminal" ]; then
        mkdir -p /usr/share/konsole
        cp -f "${THEME_PACK_DIR}"/17-Terminal/*.colorscheme /usr/share/konsole/ || true
    fi
    
    # 10. Wallpapers
    if [ -d "${THEME_PACK_DIR}/02-Wallpapers" ]; then
        mkdir -p /usr/share/wallpapers
        cp -Rf "${THEME_PACK_DIR}"/02-Wallpapers/* /usr/share/wallpapers/ || true
    fi
fi

# Write skeleton configs for KWin (Aurorae decoration theme)
cat > "${KWINRC}" <<'EOF_KWINRC'
[org.kde.kdecoration2]
library=org.kde.aurorae
theme=HomeAurelia
EOF_KWINRC

# Write skeleton configs for kcminputrc (cursors theme)
cat > "${KCMINPUTRC}" <<'EOF_KCMINPUTRC'
[Mouse]
cursorTheme=home-aurelia-cursors
EOF_KCMINPUTRC

# Write Kvantum system-wide and user configuration files
mkdir -p "$(dirname "${KVANTUMRC}")"
cat > "${KVANTUMRC}" <<'EOF_KVANTUM'
[General]
theme=HomeAurelia
EOF_KVANTUM

cp "${KDEGLOBALS}" /etc/xdg/kdeglobals
chmod 0644 "${PRESENTATION_LOCK}" "${PRESENTATION_JSON}" "${COLOR_SCHEME}" "${ICON_THEME_DIR}/index.theme" "${KDEGLOBALS}" /etc/xdg/kdeglobals "${PLASMARC}" "${KWINRC}" "${KCMINPUTRC}" "${KVANTUMRC}"

if [ -d "${LIVE_HOME}" ]; then
    install -d -m 0755 "${LIVE_HOME}/.config"
    cp "${KDEGLOBALS}" "${LIVE_HOME}/.config/kdeglobals" || true
    cp "${PLASMARC}" "${LIVE_HOME}/.config/plasmarc" || true
    cp "${KWINRC}" "${LIVE_HOME}/.config/kwinrc" || true
    cp "${KCMINPUTRC}" "${LIVE_HOME}/.config/kcminputrc" || true
    install -d -m 0755 "${LIVE_HOME}/.config/Kvantum"
    cp "${KVANTUMRC}" "${LIVE_HOME}/.config/Kvantum/kvantum.kvconfig" || true
    chown -R "${LIVE_USER}:${LIVE_USER}" "${LIVE_HOME}/.config" || true
fi

echo "Home Aurelia presentation lock staged."
