#!/bin/sh

set -e

lb config noauto \
    --mode debian \
    --system live \
    --architectures amd64 \
    --linux-flavours amd64 \
    --debian-installer false \
    --archive-areas "main contrib non-free non-free-firmware" \
    --apt-indices false \
    --memtest none \
    --iso-application "Phoenix OS" \
    --iso-publisher "Phoenix OS Project" \
    --iso-volume "PHOENIX_OS" \
    --binary-images iso-hybrid \
    --chroot-filesystem squashfs \
    --compression xz \
    --zsync false \
    --bootappend-live "boot=live components quiet splash username=phoenix plymouth.ignore-serial-consoles locales=en_US.UTF-8 keyboard-layouts=us" \
    --bootloader grub-efi \
    "${@}"
