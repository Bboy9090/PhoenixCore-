# Deprecated: `mobile/` React Native tree

**Status: deprecated.**

Phoenix Core’s supported mobile client is **`phoenix-core-mobile/`** (Expo), which talks to **`backend/`** (FastAPI).

Do not start new features here unless explicitly consolidating this tree into `phoenix-core-mobile/`.

See **`docs/REPO_STATUS_MAP.md`** and **`docs/AUTHORITY_MODEL.md`**.
