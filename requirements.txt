
# BootForge Requirements
# Core dependencies
# Development and packaging
# Distributed architecture dependencies
# GUI dependencies (optional)
# Linux: badblocks, e2fsprogs, dosfstools, ntfs-3g
# Note: These are system packages, install separately
# Optional Apple platform support
# Optional system tools integration
# Optional: OpenCore Legacy Patcher (macOS only, for Tools > OpenCore Legacy Patcher)
# Windows: format (built-in)
# libimobiledevice (system package for device detection, optional)
# macOS: diskutil (built-in)
# wxpython  # Uncomment to launch embedded OCLP from BootForge
Flask-CORS==4.0.0
Flask-SocketIO==5.3.4
Pillow>=10.0.0
PyQt6>=6.9.0
click>=8.0.0
colorama>=0.4.6
cryptography>=45.0.0
Flask==2.3.3
grpcio
grpcio-tools
grpcio-tools>=1.66.0
grpcio>=1.66.0
Pillow>=10.0.0
protobuf
protobuf>=5.28.0
psutil>=5.9.5
psutil>=7.0.0
pyinstaller>=6.15.0
python-engineio==4.7.1
python-socketio==5.9.0
pyyaml>=6.0.2
qrcode==7.4.2
requests==2.31.0
requests>=2.32.0
zeroconf
zeroconf>=0.132.0
