# Legacy Materials (to remove or quarantine)

This file mirrors **`docs/LEGACY.md`**. See that file for the canonical description.

**Summary:** `legacy/`, `server/` (Flask sibling-path API), and duplicate trees under `legacy/bootable_usb/` are not primary development surfaces. Active BootForge code lives under **`desktop/`**; the Rust engine lives under **`crates/`**; HTTP orchestration for new work belongs in **`backend/`**.
