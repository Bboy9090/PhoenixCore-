# Native Evidence

Create one folder per milestone (`NATIVE-0` ... `NATIVE-6`) and commit reproducible proof artifacts:

- command transcripts
- serial logs
- boot/app screenshots
- test reports
- build hashes/signature outputs

Only move a milestone to `done` in `native/roadmap.yaml` after evidence exists.
