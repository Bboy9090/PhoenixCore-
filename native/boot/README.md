# Native Boot

Bootloader and boot chain ownership area for UEFI handoff, early console, and kernel entry.

Store boot proofs under `../evidence/NATIVE-0` (serial logs, screenshots, and run commands).
