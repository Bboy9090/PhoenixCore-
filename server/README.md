# Legacy Flask server (`server/api.py`)

**Status: deprecated.**

This Flask + SocketIO API was built when BootForge lived under a different checkout layout. It has been repointed to `desktop/src` for imports, but **new work should use the FastAPI backend**:

- **`backend/main.py`** — supported Phoenix Core REST API (devices, recipes, safety, builds).

See **`docs/CANONICAL_RUNTIME.md`**.
