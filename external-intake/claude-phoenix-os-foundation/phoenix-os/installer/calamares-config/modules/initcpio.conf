# Phoenix OS — Calamares: initcpio module
# File: installer/calamares-config/modules/initcpio.conf
#
# Runs update-initramfs (Ubuntu) or mkinitcpio (Arch) in the installed chroot.
# On Ubuntu targets, this invokes update-initramfs -u -k all.

---
# Use update-initramfs for Ubuntu/Debian targets
be_quiet: false
