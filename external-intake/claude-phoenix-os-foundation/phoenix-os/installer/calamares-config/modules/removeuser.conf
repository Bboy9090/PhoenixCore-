# Phoenix OS — Calamares: removeuser module
# File: installer/calamares-config/modules/removeuser.conf
#
# Removes the live session user ('phoenix') from the installed system.
# The phoenix-postinstall.py module also handles this, but removeuser
# provides a Calamares-native safety net.

---
username: "phoenix"
