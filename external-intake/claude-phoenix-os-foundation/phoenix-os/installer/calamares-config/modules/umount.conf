# Phoenix OS — Calamares: umount module
# File: installer/calamares-config/modules/umount.conf
#
# Unmounts all filesystems mounted during installation.
# This is the final step before the install is complete.

---
# Unload kernel modules that were loaded for the install process
unloadModules: false
